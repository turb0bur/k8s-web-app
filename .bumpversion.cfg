[bumpversion]
current_version = 3.0.0
commit = True
tag = False
message = Helm chart version updated: {current_version} → {new_version}

[bumpversion:file:helm/Chart.yaml]
search = version: {current_version}
replace = version: {new_version}
